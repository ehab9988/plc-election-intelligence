[project]
name = "plc-api"
version = "0.1.0"
description = "PLC Election Intelligence API"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
